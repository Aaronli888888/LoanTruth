# LoanTruth 配置（可选）
#
# 默认走 CF Worker 代理（key 存服务端 secret，前端不暴露），无需配置即可用。
#
# 自托管两种方式：
# 1. 部署你自己的 Worker：见 worker/ 目录（wrangler deploy + secret put GLM_API_KEY）
# 2. 或直连智谱（key 会打进前端 bundle，仅本地调试用）：
#    VITE_API_ENDPOINT=https://open.bigmodel.cn/api/paas/v4/chat/completions
#    VITE_ZHIPU_API_KEY=你的智谱key

# VITE_API_ENDPOINT=你的Worker或智谱端点
# VITE_ZHIPU_API_KEY=你的智谱key（仅直连时需要）
