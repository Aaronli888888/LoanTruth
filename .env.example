# LoanTruth 配置（可选）
#
# 默认直调智谱（国内可达）。构建时注入 VITE_ZHIPU_API_KEY 即可。
#
# 海外/自托管用户可改用 CF Worker 代理（见 worker/ 目录）保护 key：
#   部署 Worker 后设 VITE_API_ENDPOINT 指向你的 Worker URL。

# VITE_ZHIPU_API_KEY=你的智谱key（demo 建议用免费额度，可设限额/定期轮换）
# VITE_API_ENDPOINT=https://open.bigmodel.cn/api/paas/v4/chat/completions
